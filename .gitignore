# base de datos local (estado editable)
bg.db
bg.db-wal
bg.db-shm

# config local con API key
config.json

# python
__pycache__/
*.pyc
.venv/

# DATOS PERSONALES — nunca al repo. Cada quien tiene su colección local; el repo solo trae
# el preseed genérico del top-5000 (data/bgg_top.json).
# (los comentarios van en su propia línea: git NO soporta comentarios al final de un patrón)
# tu export de BGG (ownership, ratings, wishlist, comentarios):
collection.csv
# cache enriquecido de TU colección (revela tu set de juegos):
data/bgg_data.json

# intermedios del horneado del seed, backups y archivos retirados (nunca al repo):
data/_*.json
data/*.tmp
data/*.bak
data/*.retired
PENDING.md
